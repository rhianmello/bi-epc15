(function () {
  // Preenchido quando o projeto Supabase exclusivo do BI for criado.
  // A publishable key pode ficar no frontend; nunca use service_role aqui.
  window.EPC15_SUPABASE_CONFIG = {
    enabled: false,
    url: '',
    publishableKey: '',
    schemaVersion: 'epc15_snapshot_v1'
  };
}());
